package view;

import model.Body;

import java.awt.*;

public class BodyPanel extends ElementPanel {
    public BodyPanel(Body element) {
        super(element);
        setBackground(Color.GREEN);
    }
}
