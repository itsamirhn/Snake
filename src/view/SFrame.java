package view;

import javax.swing.*;

public class SFrame extends JFrame {
    public SFrame() {
        super("Snake");
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        setVisible(true);
    }
}
